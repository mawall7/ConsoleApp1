using System;
using System.Collections.Generic;
using System.Text;

namespace Garage
{
    interface IUI<T>
    {
        T input();

    }
    class UI
    {
        
        public void Mainmenu()
        {
            // meny
            string key;
            key = null;
            if (string.IsNullOrWhiteSpace(key)) 
            do
            {
                switch (key)
                {
                    default:
                        break;
                }

            }
            while (true);

    
        }
        
    }
}
